#include "Graph.h"
#include "Kreis.h"

int main() {
    // Kreis kann weiter erstellt werden
    Kreis kreis = Kreis(5);
    // Graph kann einzeln nicht erstellt werden
    // Graph graph = Graph(5, 20);
}
